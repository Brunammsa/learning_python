from random import randint

lancamento_dado = int(input('Quantas vezes o dado irá ser lançado? >>> '))
dados = {'d1': randint(1, 6),
         'd2': randint(1, 6)} # O {} é importado da biblioteca


'''for i in range(lancamento_dado):
    print(dados)
    if #como vou usar os d1 e d2 se não são variáveis?'''